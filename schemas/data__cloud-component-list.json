{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/ComponentListResult",
  "definitions": {
    "ComponentListResult": {
      "type": "object",
      "properties": {
        "components": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ComponentSummary"
          }
        }
      },
      "required": ["components"],
      "additionalProperties": false,
      "description": "Shape returned by `sf data-cloud component list --json`."
    },
    "ComponentSummary": {
      "type": "object",
      "properties": {
        "componentName": {
          "type": "string",
          "description": "API/developer name of the component, e.g. \"HighValueCustomers\"."
        },
        "lastModifiedDate": {
          "type": "string",
          "description": "ISO-8601 timestamp of the component's last modification."
        }
      },
      "required": ["componentName", "lastModifiedDate"],
      "additionalProperties": false,
      "description": "One component row, exactly as the component-list endpoint returns it (§5.4)."
    }
  }
}
